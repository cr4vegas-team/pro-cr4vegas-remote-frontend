import { environment } from 'src/environments/environment';

export const GLOBAL = {
    API: environment.api,
    FUNCTION_NOT_ALLOWED: 'Función no disponible por el momento',
    IMAGE_DEFAULT: 'assets/img/default.jpg'
}
