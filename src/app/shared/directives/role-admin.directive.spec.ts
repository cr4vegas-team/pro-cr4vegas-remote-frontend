import { RoleAdminDirective } from './role-admin.directive';

describe('RoleAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleAdminDirective();
    expect(directive).toBeTruthy();
  });
});
