import { UnitActiveFilterPipe } from '../unit-active-filter.pipe';

describe('UnitActiveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitActiveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
