import { UnitCommunicationFilterPipe } from '../unit-communication-filter.pipe';

describe('UnitCommunicationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitCommunicationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
