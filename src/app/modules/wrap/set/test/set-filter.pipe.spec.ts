import { SetFilterPipe } from '../set-filter.pipe';

describe('SetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
