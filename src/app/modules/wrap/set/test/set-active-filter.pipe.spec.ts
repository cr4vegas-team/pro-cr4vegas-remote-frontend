import { SetActiveFilterPipe } from '../set-active-filter.pipe';

describe('SetActiveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SetActiveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
