import { SectorActiveFilterPipe } from '../sector-active-filter.pipe';

describe('SectorActiveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SectorActiveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
