import { SectorFilterPipe } from '../sector-filter.pipe';

describe('SectorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SectorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
